# UEPL driver-profiling / equipment-analysis backend
# Python 3.10+  (uses PEP 604 unions e.g. `str | None`)

google-genai>=1.0.0        # new unified Gemini SDK (from google import genai)
fastapi>=0.110.0           # HTTP API the frontend calls
uvicorn[standard]>=0.29.0  # ASGI server (dev)
python-multipart>=0.0.9    # multipart/form-data (video upload)
python-dotenv>=1.0.0       # auto-load backend/.env (so keys never go on the CLI)

# Production server on GCP Cloud Run (aligned with the AFDD stack):
#   gunicorn>=21.2.0
